# ============================================
# OmniAI — Procfile (for Railway, Heroku, Render)
# Single web process serving both API and frontend
# ============================================
web: cd server && npm ci --omit=dev && node src/index.js